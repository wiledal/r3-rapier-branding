import React from "react";
interface R3RapierBrandingProps {
    colors?: [colorA: string, colorB: string];
    title: string;
    version: string;
    children?: React.ReactNode;
}
export declare const R3RapierBranding: ({ colors, title, version, children, }: R3RapierBrandingProps) => JSX.Element;
export {};
